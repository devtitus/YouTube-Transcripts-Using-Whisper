# Local ASR Service Configuration
LOCAL_ASR_BASE_URL=http://localhost:5689
LOCAL_ASR_MODEL=base
DEFAULT_MODEL=base
LOCAL_CHUNK_SECONDS=90
LOCAL_MAX_FILE_MB=100
LOCAL_TIMEOUT_MS=3600000

# Python Service Configuration (passed to the container)
# These control the faster-whisper model loading
FW_DEVICE=cpu
FW_COMPUTE_TYPE=int8

# Server Configuration
PORT=5688

# Paths (usually auto-detected, no need to set)
# AUDIO_DIR=./audio_file
# FFMPEG_CMD=ffmpeg