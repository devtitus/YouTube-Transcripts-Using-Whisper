# Docker Environment Configuration
# Copy this to .env to customize the service

# Local ASR Service Configuration
LOCAL_ASR_MODEL=base.en
LOCAL_CHUNK_SECONDS=600
LOCAL_MAX_FILE_MB=100
LOCAL_TIMEOUT_MS=1800000

# Python Service Configuration
# These control the faster-whisper model loading in the container
FW_DEVICE=cpu
FW_COMPUTE_TYPE=int8

# Note: The following are set automatically in docker-compose.yml
# and generally do not need to be overridden.
# NODE_ENV=production
# PORT=5688
# LOCAL_ASR_BASE_URL=http://localhost:5689